export enum State {
  login = "login",
  views = "views",
  pages = "pages",
  durations = "durations",
  languages = "languages",
  oses = "oses",
  platforms = "platforms",
  settings = "settings"
}

export enum Modal {
  close = "close",
  delete = "delete",
  rename = "rename",
  add = "add"
}

export enum Charts {
  bar = "bar",
  pie = "pie",
  bubble = "bubble"
}

export enum ViewsEnum {
  unique = "unique",
  total = "total"
}

export enum DurationsEnum {
  average = "average",
  detailed = "detailed"
}

export enum RadioChoices {
  selected = "selected",
  all = "all"
}
