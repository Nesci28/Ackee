export enum State {
  login = "login",
  views = "views",
  pages = "pages",
  durations = "durations",
  languages = "languages",
  oses = "oses",
  platforms = "platforms",
  settings = "settings"
}

export enum Modal {
  close = "close",
  delete = "delete",
  rename = "rename",
  add = "add"
}

export enum Charts {
  bar = "bar",
  pie = "pie"
}
