export enum State {
  login = "login",
  views = "views",
  pages = "pages",
  durations = "durations",
  events = "events",
  languages = "languages",
  oses = "oses",
  platforms = "platforms",
  settings = "settings"
}

export enum Modal {
  close = "close",
  delete = "delete",
  rename = "rename",
  add = "add"
}

export enum Charts {
  bar = "bar",
  pie = "pie",
  bubble = "bubble",
  line = "line"
}

export enum ViewsEnum {
  unique = "unique",
  total = "total",
  time = "time"
}

export enum DurationsEnum {
  average = "average",
  detailed = "detailed"
}

export enum RadioChoices {
  selected = "selected",
  all = "all"
}
